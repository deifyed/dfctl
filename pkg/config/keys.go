package config

const (
	// LogLevel defines the key for log level configuration
	LogLevel = "LogLevel"
	// DotFilesDir defines the key for the directory where dotfiles (and directories) should be placed
	DotFilesDir = "DotFilesDir"
)
